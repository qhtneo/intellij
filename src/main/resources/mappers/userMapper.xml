<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.trip.dao.UserDao">

    <!-- 회원정보추가 -->
    <insert id = "insertUser" parameterType = "User">
        INSERT INTO
            MEMBER	(USER_NAME, USER_PW, USER_NICKNAME, USER_EMAIL)
        VALUES
            (#{userName}, #{userPw}, #{userNickname}, #{userEmail})
    </insert>
</mapper>
